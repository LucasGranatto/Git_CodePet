{
  "name": "codepet",
  "version": "1.0.0",
  "description": "A virtual pet whose mood reflects your GitHub coding activity, rendered as a live SVG for your profile README.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "dev": "vercel dev"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
